name: Supabase Keepalive
on:
  schedule:
    - cron: '0 2 * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase
        run: |
          curl -s \
            -H "apikey: sb_publishable_..." \
            "https://liulrnuqlpreqvymqcll..."
          echo "Pinged at $(date)"
